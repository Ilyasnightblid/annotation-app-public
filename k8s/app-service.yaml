apiVersion: v1
kind: Service
metadata:
  name: annotation-app-service
spec:
  selector:
    app: annotation-app
  type: NodePort
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30007
